repos:
  - repo: local
    hooks:
      - id: strip-notebook-outputs
        name: Strip notebook outputs and execution state
        entry: python3 tools/strip_notebook_outputs.py
        language: system
        files: \.ipynb$
